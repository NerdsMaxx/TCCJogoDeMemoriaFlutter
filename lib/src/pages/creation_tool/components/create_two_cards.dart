import 'package:flutter/material.dart';

class CreateTwoCards extends StatelessWidget {
  const CreateTwoCards({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
